const { createNewCore } = require('./create_core')
const { loadCore } = require('./load_core')
const { removeCore } = require('./remove_core')

module.exports = { createNewCore, loadCore, removeCore }
